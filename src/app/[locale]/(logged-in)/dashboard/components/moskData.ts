export type DataStakesAndClaims = {
      aviableToClaim: string;
      claims: string;
      activeStakes: string;
      stakes: string;
      unStakes: string;
  };

export const dataStakesAndClaims = {
    aviableToClaim: "0",
    claims: "0",
    activeStakes: "0",
    stakes: "0",
    unStakes: "0"
}